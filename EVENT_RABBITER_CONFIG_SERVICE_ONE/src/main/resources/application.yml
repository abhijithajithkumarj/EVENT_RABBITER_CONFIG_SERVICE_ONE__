server:
  port: 9999

spring:
  application:
    name: CONFIG-SERVER
  profiles:
    active: native
  cloud:
    config:
      server:
#        git:
#          url:
#          search-paths:
#            - src/main/resources/config
#          default-label: master
#          timeout: 5
#          clone-on-start: true
#          force-pull: true

        native:
          search-locations: "classpath:/config"

